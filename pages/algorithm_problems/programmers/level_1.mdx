# 프로그래머스 Lv.1

Lv.1 문제 모아보기:
https://school.programmers.co.kr/learn/challenges?order=recent&levels=1&page=1&languages=javascript

## 달리기 경주

https://school.programmers.co.kr/learn/courses/30/lessons/178871

```js
function solution(players, callings) {
  var answer = []
  answer = Array.from(players)

  let temp_players = {}
  for (let i = 0; i < players.length; i++) {
    temp_players[players[i]] = i
  }

  for (let calling of callings) {
    let num = temp_players[calling]
    let temp = answer[num]
    answer[num] = answer[num - 1]
    answer[num - 1] = temp

    temp_players[answer[num]] = num
    temp_players[answer[num - 1]] = num - 1
  }

  return answer
}
```

## 추억 점수

https://school.programmers.co.kr/learn/courses/30/lessons/176963

```js
function solution(name, yearning, photo) {
  var answer = []

  let table = {}
  for (let i = 0; i < name.length; i++) {
    table[name[i]] = yearning[i]
  }

  for (let i = 0; i < photo.length; i++) {
    let score = 0
    for (let j = 0; j < photo[i].length; j++) {
      if (!table[photo[i][j]]) continue
      score += table[photo[i][j]]
    }
    answer.push(score)
  }

  return answer
}
```

## 공원 산책

https://school.programmers.co.kr/learn/courses/30/lessons/172928

```js
function solution(park, routes) {
  var answer = []

  let x, y

  for (let i = 0; i < park.length; i++) {
    for (let j = 0; j < park[i].length; j++) {
      if (park[i][j] === 'S') {
        x = i
        y = j
        break
      }
    }
  }

  for (let i = 0; i < routes.length; i++) {
    const dir = routes[i][0]
    const num = parseInt(routes[i][2])

    if (dir === 'N') {
      if (park[x - num]) {
        let obstacle = false
        for (let i = 1; i <= num; i++) {
          if (park[x - i][y] === 'X') {
            obstacle = true
            break
          }
        }
        if (obstacle) continue
        else x -= num
      } else continue
    } else if (dir === 'S') {
      if (park[x + num]) {
        let obstacle = false
        for (let i = 1; i <= num; i++) {
          if (park[x + i][y] === 'X') {
            obstacle = true
            break
          }
        }
        if (obstacle) continue
        else x += num
      } else continue
    } else if (dir === 'W') {
      if (park[x][y - num]) {
        let obstacle = false
        for (let i = 1; i <= num; i++) {
          if (park[x][y - i] === 'X') {
            obstacle = true
            break
          }
        }
        if (obstacle) continue
        else y -= num
      } else continue
    } else if (dir === 'E') {
      if (park[x][y + num]) {
        let obstacle = false
        for (let i = 1; i <= num; i++) {
          if (park[x][y + i] === 'X') {
            obstacle = true
            break
          }
        }
        if (obstacle) continue
        else y += num
      } else continue
    }
  }

  answer.push(x, y)

  return answer
}
```

## 바탕화면 정리

https://school.programmers.co.kr/learn/courses/30/lessons/161990

```js
function solution(wallpaper) {
  var answer = []

  let lux = (luy = 50)
  let rdx = (rdy = 0)

  for (let i = 0; i < wallpaper.length; i++) {
    for (let j = 0; j < wallpaper[i].length; j++) {
      if (wallpaper[i][j] === '#') {
        if (lux > i) lux = i
        if (luy > j) luy = j
        if (i > rdx) rdx = i
        if (j > rdy) rdy = j
      }
    }
  }

  answer.push(lux, luy, rdx + 1, rdy + 1)
  return answer
}
```

## 덧칠하기

https://school.programmers.co.kr/learn/courses/30/lessons/161989

```js
function solution(n, m, section) {
  var answer = 0

  let temp = 0
  for (let i = 0; i < section.length; i++) {
    if (temp >= section[i]) continue
    temp = section[i] + m - 1
    answer++
  }
  return answer
}
```

## 대충 만든 자판

https://school.programmers.co.kr/learn/courses/30/lessons/160586

```js
function solution(keymap, targets) {
  var answer = []

  for (let i = 0; i < targets.length; i++) {
    let count = 0
    let found = true
    for (let j = 0; j < targets[i].length; j++) {
      let min = 101

      for (let k = 0; k < keymap.length; k++) {
        for (let l = 0; l < keymap[k].length; l++) {
          if (keymap[k][l] === targets[i][j]) {
            if (l < min) {
              min = l + 1
              break
            }
          }
        }
      }
      if (min === 101) {
        found = false
        break
      } else count += min
    }

    if (found) answer.push(count)
    else answer.push(-1)
  }
  return answer
}
```

## 카드 뭉치

https://school.programmers.co.kr/learn/courses/30/lessons/159994

```js
function solution(cards1, cards2, goal) {
  var answer = 'Yes'

  let card1_index = 0
  let card2_index = 0

  for (let i = 0; i < goal.length; i++) {
    if (cards1[card1_index] !== goal[i] && cards2[card2_index] !== goal[i]) {
      return 'No'
    }

    if (cards1[card1_index] === goal[i]) {
      card1_index++
    }
    if (cards2[card2_index] === goal[i]) {
      card2_index++
    }
  }
  return answer
}
```

## 둘만의 암호

https://school.programmers.co.kr/learn/courses/30/lessons/155652

```js
function solution(s, skip, index) {
  var answer = ''

  let checkList = {}

  for (let i = 0; i < skip.length; i++) {
    checkList[skip[i]] = true
  }

  for (let i = 0; i < s.length; i++) {
    let charCode = s.charCodeAt(i)
    for (let j = index; j > 0; j--) {
      charCode += 1
      if (charCode === 123) charCode = 97
      if (checkList[String.fromCharCode(charCode)]) j++
    }

    answer += String.fromCharCode(charCode)
  }
  return answer
}
```

## 개인정보 수집 유효기간

https://school.programmers.co.kr/learn/courses/30/lessons/150370

```js
function solution(today, terms, privacies) {
  var answer = []

  const todayArr = today.split('.')
  const todayYear = parseInt(todayArr[0])
  const todayMonth = parseInt(todayArr[1])
  const todayDay = parseInt(todayArr[2])

  const termsTable = {}
  for (let i = 0; i < terms.length; i++) {
    const termsArr = terms[i].split(' ')
    termsTable[termsArr[0]] = termsArr[1]
  }

  for (let i = 0; i < privacies.length; i++) {
    const privaciesArr = privacies[i].split(' ')
    const dateArr = privaciesArr[0].split('.')

    let expireYear = parseInt(dateArr[0])
    let expireMonth = parseInt(dateArr[1])
    let expireDay = parseInt(dateArr[2])

    const passMonth = parseInt(termsTable[privaciesArr[1]] % 12)
    const passYear = parseInt(termsTable[privaciesArr[1]] / 12)
    expireMonth += passMonth
    expireYear += passYear

    if (expireMonth > 12) {
      expireYear += 1
      expireMonth -= 12
    }

    expireDay -= 1
    if (expireDay === 0) {
      expireDay = 28
      expireMonth -= 1
      if (expireMonth === 0) {
        expireMonth = 12
        expireYear -= 1
      }
    }

    if (expireYear < todayYear) {
      answer.push(i + 1)
    } else if (expireYear === todayYear) {
      if (expireMonth < todayMonth) {
        answer.push(i + 1)
      } else if (expireMonth === todayMonth) {
        if (expireDay < todayDay) {
          answer.push(i + 1)
        }
      }
    }
  }
  return answer
}
```

## 크기가 작은 부분 문자열

https://school.programmers.co.kr/learn/courses/30/lessons/147355

```js
function solution(t, p) {
  var answer = 0

  for (let i = 0; i <= t.length - p.length; i++) {
    if (parseInt(t.substring(i, i + p.length)) <= parseInt(p)) answer++
  }
  return answer
}
```

## 가장 가까운 같은 글자

https://school.programmers.co.kr/learn/courses/30/lessons/142086

```js
function solution(s) {
  var answer = []

  for (let i = 0; i < s.length; i++) {
    for (let j = i - 1; j >= -1; j--) {
      if (j === -1) {
        answer.push(j)
        break
      }

      if (s[i] === s[j]) {
        answer.push(i - j)
        break
      }
    }
  }
  return answer
}
```

## 문자열 나누기

https://school.programmers.co.kr/learn/courses/30/lessons/140108

```js
function solution(s) {
  var answer = 0

  for (let i = 0; i < s.length; i++) {
    let same = 1
    let diff = 0
    for (let j = i + 1; j <= s.length; j++) {
      if (s[i] === s[j]) same++
      else diff++

      if (same === diff) {
        answer++
        i = j
        break
      }
      if (j === s.length) {
        i = j
        answer++
        break
      }
    }
  }
  return answer
}
```

## 명예의 전당(1)

https://school.programmers.co.kr/learn/courses/30/lessons/138477

```js
function solution(k, score) {
  var answer = []

  let temp = []
  for (let i = 0; i < score.length; i++) {
    if (temp.length >= k) {
      if (score[i] > temp[0]) {
        temp.push(score[i])
        temp.shift()
      }
    } else temp.push(score[i])
    temp.sort((a, b) => a - b)

    answer.push(temp[0])
  }
  return answer
}
```

## \*기사단원의 무기

https://school.programmers.co.kr/learn/courses/30/lessons/136798

```js
function solution(number, limit, power) {
  var answer = 0

  for (let i = 1; i <= number; i++) {
    let count = 0
    for (let j = 1; j <= Math.floor(Math.sqrt(i)); j++) {
      if (i % j === 0) {
        if (i / j === j) count++
        else count += 2
      }
    }

    if (count > limit) answer += power
    else answer += count
  }

  return answer
}
```

## 과일 장수

https://school.programmers.co.kr/learn/courses/30/lessons/135808

```js
function solution(k, m, score) {
  var answer = 0

  const box = score.sort((a, b) => a - b)

  for (let i = box.length - m; i >= 0; ) {
    answer += box[i] * m
    i -= m
  }
  return answer
}
```

## 푸트 파이트 대회

https://school.programmers.co.kr/learn/courses/30/lessons/134240

```js
function solution(food) {
    var answer = '';
    
    for(let i=1;i<food.length;i++) {
        answer += `${i}`.repeat(food[i]/2);
    }
    answer += '0'
    for(let i=answer.length-2;i>=0;i--) {
        answer += answer[i];
    }
    
    return answer;
}
```

## *햄버거 만들기

https://school.programmers.co.kr/learn/courses/30/lessons/133502

```js
function solution(ingredient) {
    var answer = 0;
    
    const stack = [];
    
    for(let i=0;i<ingredient.length;i++) {
        stack.push(ingredient[i]);
        if(stack.length >3 && stack.slice(stack.length-4,stack.length).join('') === '1231') {
            answer++;
            for(let j=0;j<4;j++) stack.pop();
        }
    }
    
    return answer;
}
```

## *옹알이 (2)

https://school.programmers.co.kr/learn/courses/30/lessons/133499

```js
function solution(babbling) {
    var answer = 0;
    
    const table = ['aya','ye','woo','ma'];
    
    for(let i=0;i<babbling.length;i++) {
        let repeat ='';
        
        while(babbling[i].length) {
            let flag = 1;
            for(let word of table) {
                if(babbling[i].slice(0,word.length) === word && word !== repeat ) {
                    flag = 0;
                    repeat = word;
                    babbling[i] = babbling[i].replace(babbling[i].slice(0,word.length),'')                    
                }
            }
            if(flag) break;
        }
        
        if(babbling[i].length === 0) answer++;
    }
    
    return answer;
}
```

## 콜라 문제

https://school.programmers.co.kr/learn/courses/30/lessons/132267

```js
function solution(a, b, n) {
    var answer = 0;
    
    while(Math.floor(n/a)) {
        let rest = n % a;
        answer += Math.floor(n/a) * b ;
        n = Math.floor(n/a)*b + rest;
    }
    
    return answer;
}
```

## 삼총사

https://school.programmers.co.kr/learn/courses/30/lessons/131705

```js
function solution(number) {
    var answer = 0;
    
    const temp = {}
    for(let i=0;i<number.length;i++) {
        temp[-number[i]] = true;
        
        for(let j=i+1;j<number.length;j++) {
            for(let k=j+1;k<number.length;k++) {
                if(temp[number[j]+number[k]]) answer++;
            }
        }
        
        temp[-number[i]] = false;
    }
    
    return answer;
}
```

## 숫자 짝꿍

https://school.programmers.co.kr/learn/courses/30/lessons/131128

```js
function solution(X, Y) {
    var answer = '';
    
    const table = {}
    const Xtable = {}
    const Ytable = {}
    
    for(let num of X) {
        if(Xtable[num]) Xtable[num]++;
        else Xtable[num] = 1;
    }
    for(let num of Y) {
        if(Ytable[num]) Ytable[num]++;
        else Ytable[num] = 1;
    }
    for(let num in Ytable) {
        if(Xtable[num]) {
            if(Xtable[num] <= Ytable[num]) table[num] = Xtable[num];
            else table[num] = Ytable[num];
        }
    }

    const keyArray = Object.keys(table); 
    if(keyArray.length === 0) return '-1';
    if(keyArray.length === 1 && keyArray[0] === '0') return '0';
    
    for(let num of keyArray.sort((a,b) => b-a)) {
        for(let i=0;i<table[num];i++) {
            answer += num;
        }
    }
    
    return answer;
}
```

## 성격 유형 검사하기

https://school.programmers.co.kr/learn/courses/30/lessons/118666

```js
function solution(survey, choices) {
    var answer = '';
    
    const table = {
        R:0,
        T:0,
        C:0,
        F:0,
        J:0,
        M:0,
        A:0,
        N:0
    }
    
    for(let i=0;i<survey.length;i++) {
        const choice = choices[i];
        if(choice === 1 || choice === 2 || choice === 3) {
            const firstChar = survey[i][0];
            
            if(choice === 1) table[firstChar] += 3;
            if(choice === 2) table[firstChar] += 2;
            if(choice === 3) table[firstChar] += 1;
        }
        else if(choice === 5 || choice === 6 || choice === 7) {
            const secondChar = survey[i][1];
            if(choice === 5) table[secondChar] += 1;
            if(choice === 6) table[secondChar] += 2;
            if(choice === 7) table[secondChar] += 3;
        }
    }
    
    if(table['R'] >= table['T']) answer += 'R';
    else answer += 'T';
    
    if(table['C'] >= table['F']) answer += 'C';
    else answer += 'F';
    
    if(table['J'] >= table['M']) answer += 'J';
    else answer += 'M';
    
    if(table['A'] >= table['N']) answer += 'A';
    else answer += 'N';
    
    return answer;
}
```

## 신고 결과 받기

https://school.programmers.co.kr/learn/courses/30/lessons/92334

```js
function solution(id_list, report, k) {
    var answer = [];
    
    const reporterTable = {}
    const table = {}
    for(let i=0;i<report.length;i++) {
        const reportArr = report[i].split(' ');
        const reporter = reportArr[0];
        const reported = reportArr[1];
        
        if(table[reported]) {
            if(table[reported]['reportUsers'][reporter]) continue;
            table[reported]['reportUsers'][reporter] = true;
            table[reported]['count']++;
        }
        else {
            table[reported] = {
                count: 1,
                reportUsers: {
                    [reporter]:true
                }
            }
        }
    }
    
    for(reported in table) {
        if((table[reported]['count']) >= k ) {
            for(user in table[reported]['reportUsers']) {
                if(reporterTable[user]) reporterTable[user]++;
                else reporterTable[user] = 1;
            }
        }
    }
    
    for(id of id_list) {
        if(reporterTable[id]) answer.push(reporterTable[id])
        else answer.push(0);
    }
    
    return answer;
}
```

## 나머지가 1이 되는 수 찾기

https://school.programmers.co.kr/learn/courses/30/lessons/87389

```js
function solution(n) {
    var answer = 0;
    
    while(n%answer !== 1) {
        answer++;
    }
    
    return answer;
}
```

## *최소직사각형

https://school.programmers.co.kr/learn/courses/30/lessons/86491

```js
function solution(sizes) {
    var answer = 0;
    
    let max = 0;
    let min = 0;
    for(let size of sizes) {
        if(size[0] >= size[1]) {
            if(size[0] >= max) {
                max = size[0];
            }
                if(size[1] > min) min = size[1];
        }
        else {
            if(size[1] >= max) {
                max = size[1];
            }
            if(size[0] > min) min = size[0];
        }
    }
    
    answer = max * min;
    
    return answer;
}
```

## 없는 숫자 더하기

https://school.programmers.co.kr/learn/courses/30/lessons/86051

```js
function solution(numbers) {
    var answer = 45;
    
    for(number of numbers) answer -= number;
    
    return answer;
}
```

## 부족한 금액 계산하기

https://school.programmers.co.kr/learn/courses/30/lessons/82612

```js
function solution(price, money, count) {
    var answer = -1;
    
    const fee = price*count*(count+1)/2
    if(money >= fee) answer = 0;
    else answer = fee - money
    
    return answer;
}
```

## 숫자 문자열과 영단어

https://school.programmers.co.kr/learn/courses/30/lessons/81301

```js
function solution(s) {
    var answer = s;
    const table = {'zero':0,'one':1,'two':2,'three':3,'four':4,'five':5,
                  'six':6,'seven':7,'eight':8,'nine':9};
    
    for(let word in table) {
        answer = answer.replaceAll(word,table[word]);
    }
    answer = parseInt(answer);
    
    return answer;
}
```

## 약수의 개수와 덧셈

https://school.programmers.co.kr/learn/courses/30/lessons/77884

```js
function solution(left, right) {
    var answer = 0;
    
    for(let i=left;i<=right;i++) {
        let count = 0;
        
        for(let j=1;j<=i;j++) {
            if(i%j === 0) count++;
        }

        if(count%2 === 0) answer += i;
        else answer -= i;
    }
    return answer;
}
```

## 로또의 최고 순위와 최저 순위

https://school.programmers.co.kr/learn/courses/30/lessons/77484

```js
function solution(lottos, win_nums) {
    var answer = [];
    
    const rank_table = {6:1,5:2,4:3,3:4,2:5,1:6,0:6}
    
    const table = {};
    for(win_num of win_nums) {
        table[win_num] = true;
    }
    
    let count = 0;
    let extra_count = 0;
    for(num of lottos) {
        if(num === 0) extra_count++;
        else if(table[num]) count++;
    }
    
    answer.push(rank_table[count+extra_count],rank_table[count]);
    
    return answer;
}
```

## 음양 더하기

https://school.programmers.co.kr/learn/courses/30/lessons/76501

```js
function solution(absolutes, signs) {
    var answer = 0;
    
    for(let i=0;i<absolutes.length;i++) {
        if(signs[i]) answer += absolutes[i];
        else answer -= absolutes[i];
    }
    
    return answer;
}
```

## 신규 아이디 추천

https://school.programmers.co.kr/learn/courses/30/lessons/72410

```js
function solution(new_id) {
    var answer = '';
    
    for(let i=0;i<new_id.length;i++) {
        let code = new_id.charCodeAt(i);
        
        if(code >= 65 && code <= 90) {
            code += 32;
            new_id = new_id.replace(new_id[i],String.fromCharCode(code));
        }
                
        if(!((code >=97 && code <=122) || (code >=48 && code <= 57) || code === 45 || code === 95 || code === 46)) {
            new_id = new_id.replace(new_id[i],'');
            i--;
        }
    }
    
    for(let i=0;i<new_id.length;i++) {
        if(new_id[i] === '.' && new_id[i+1] === '.') {
            new_id = new_id.replace('..','.');
            i--;
        }
    }
    
    const arr = Array.from(new_id);
    if(arr[0] === '.') arr.shift();
    if(arr[arr.length-1] === '.') arr.pop();
    
    if(arr.length === 0) arr.push('a');
    
    if(arr.length >= 16) arr.splice(15);
    if(arr[arr.length-1] === '.') arr.pop();
    
    if(arr.length <= 2) {
        const last = arr[arr.length-1];
        while(arr.length < 3) arr.push(last);
    }
    answer = arr.join('');
    
    return answer;
}
```

## 내적

https://school.programmers.co.kr/learn/courses/30/lessons/70128

```js
function solution(a, b) {
    var answer = 0;
    
    for(let i=0;i<a.length;i++) answer += a[i] * b[i];

    return answer;
}
```

## 3진법 뒤집기

https://school.programmers.co.kr/learn/courses/30/lessons/68935

```js
function solution(n) {
    var answer = 0;
    
    let num = '';
    while(n>0) {
        num += n%3 ;
        n = Math.floor(n/3);
    }
    
    for(let i=num.length-1,j=0;i>=0;i--,j++) {
        answer += Number(num[i])*Math.pow(3,j);
    }
    
    return answer;
}
```

## 두 개 뽑아서 더하기

https://school.programmers.co.kr/learn/courses/30/lessons/68644

```js
function solution(numbers) {
    var answer = [];
    
    const table = {}
    for(let i=0;i<numbers.length;i++) {
        for(let j=i+1;j<numbers.length;j++) {
            if(table[numbers[i]+numbers[j]]) continue;
            else {
                table[numbers[i]+numbers[j]] = true;
                answer.push(numbers[i]+numbers[j]);
            } 
        }
    }

    answer.sort((a,b) => a-b);
    
    return answer;
}
```

## 키패드 누르기

https://school.programmers.co.kr/learn/courses/30/lessons/67256

```js
function solution(numbers, hand) {
    var answer = '';
    
    const table = {
        1: {2:1,5:2,8:3,0:4},
        4: {2:2,5:1,8:2,0:3},
        7: {2:3,5:2,8:1,0:2},
        3: {2:1,5:2,8:3,0:4},
        6: {2:2,5:1,8:2,0:3},
        9: {2:3,5:2,8:1,0:2},
        2: {2:0,5:1,8:2,0:3},
        5: {2:1,5:0,8:1,0:2},
        8: {2:2,5:1,8:0,0:1},
        0: {2:3,5:2,8:1,0:0},
        '*': {2:4,5:3,8:2,0:1},
        '#': {2:4,5:3,8:2,0:1}
    }
    
    let left ='*';
    let right = '#';
    for(number of numbers) {
        if(number === 1 || number === 4 || number === 7) {
            answer += 'L';
            left = number;
        }
        else if(number === 3 || number === 6 || number === 9) {
            answer += 'R';
            right = number;
        }
        else {
            const leftCount = parseInt(table[left][number]);
            const rightCount = parseInt(table[right][number]);
            if(leftCount < rightCount) {
                answer += 'L';
                left = number;
            }
            else if(leftCount > rightCount) {
                answer += 'R';
                right = number;
            }
            else {
                if(hand === 'left') {
                    answer += 'L';
                    left = number;
                }
                else {
                    answer += 'R';
                    right = number;
                }
            }
        }
    }
    
    return answer;
}
```

## 크레인 인형뽑기 게임

https://school.programmers.co.kr/learn/courses/30/lessons/64061

```js
function solution(board, moves) {
    var answer = 0;
    
    const basket = [];
    for(let move of moves) {
        for(let i=0;i<board.length;i++) {
            const boardTop = board[i][move-1];
            if(boardTop) {
                const basketTop = basket.length-1
                if(basket[basketTop] && basket[basketTop] === boardTop) {
                    answer += 2;
                    basket.pop();
                }
                else basket.push(boardTop);
                
                board[i][move-1] = 0;
                break;
            }
        }
    }
    
    return answer;
}
```

## *실패율

https://school.programmers.co.kr/learn/courses/30/lessons/42889

```js
function solution(N, stages) {
    var answer = [];
    
    const failArr = Array(N+2).fill(0);
    const successArr = Array(N+2).fill(0);
    
    for(stage of stages) failArr[stage]++;

    for(let i=1;i<failArr.length;i++) {
        for(let j=1;j<=i;j++) {
            successArr[j] += failArr[i];
        }
    }
    
    for(let i=1;i<=N;i++) answer.push([i,failArr[i]/successArr[i]]);

    answer.sort((a,b) => b[1]-a[1]);
    answer = answer.map(item => item[0]);
    
    return answer;
}
```

## *체육복

https://school.programmers.co.kr/learn/courses/30/lessons/42862

```js
function solution(n, lost, reserve) {
    var answer = 0;
    
    answer = n - lost.length;
    
    lost.sort((a,b) => a-b);
    reserve.sort((a,b) => a-b);
    
    const table = {}
    for(let i=0;i<lost.length;i++) table[lost[i]] = true;

    for(let i=0;i<reserve.length;i++) {
        if(table[reserve[i]]) {
            table[reserve[i]] = false;
            answer++;
            reserve = reserve.filter((item) => item !== reserve[i]);  
            i--;
        }
    }
    
    for(let i=0;i<reserve.length;i++) {
        if(table[reserve[i]-1] || table[reserve[i]+1]) {
            if(table[reserve[i]-1]) table[reserve[i]-1] = false;
            else if(table[reserve[i]+1]) table[reserve[i]+1] = false;
            answer++;
        }    
    }
    
    return answer;
}
```

## 모의고사

https://school.programmers.co.kr/learn/courses/30/lessons/42840

```js
function solution(answers) {
    var answer = [];
    
    const score = Array(4).fill(0);
    answers = answers.join('');
    const table = {
        1:'12345',
        2:'21232425',
        3:'3311224455'
    }
    
    for(let num in table) {
        if(table[num].length < answers.length) {
            table[num] = table[num].padEnd(answers.length,table[num]);
        }
        else table[num] = table[num].slice(0,answers.length);
    }
    
    for(let i=0;i<answers.length;i++) {
         if(table[1][i] === answers[i]) score[1]++;
         if(table[2][i] === answers[i]) score[2]++;
         if(table[3][i] === answers[i]) score[3]++;
    }
    answer.push([1,score[1]],[2,score[2]],[3,score[3]]);
    answer.sort((a,b) => b[1] - a[1]);
    
    const max = answer[0][1];
    if(max > answer[1][1]) answer = [answer[0][0]];
    else answer = answer.filter((ans) => ans[1] === max).map((ans) => ans[0]);

    return answer;
}
```

## K번째수

https://school.programmers.co.kr/learn/courses/30/lessons/42748

```js
function solution(array, commands) {
    var answer = [];
    
    for(command of commands) {
        const arr = array.slice(command[0]-1,command[1]);
        arr.sort((a,b) => a-b);
        answer.push(arr[command[2]-1]);
    }
    
    return answer;
}
```

## 완주하지 못한 선수

https://school.programmers.co.kr/learn/courses/30/lessons/42576

```js
function solution(participant, completion) {
    var answer = '';

    participant.sort();
    completion.sort();

    for(let i=0;i<completion.length;i++) 
        if(participant[i] !== completion[i]) return participant[i];

    answer = participant[participant.length-1];

    return answer;
}
```

## [1차] 다트 게임

https://school.programmers.co.kr/learn/courses/30/lessons/17682

```js
function solution(dartResult) {
    var answer = 0;
    
    const scores = [];
    for(let i=0;i<dartResult.length;i++) {
        
        let char = Number(dartResult[i]);
        if(isNaN(char)) {
            char = dartResult[i];
            let num = scores[scores.length-1];
            if(char === 'S') continue;
            if(char === 'D' || char === 'T') {
                if(char === 'D') num = num**2;
                else if(char === 'T') num = num**3;
            }
            else {
                if(char === '*') {
                    num *= 2;
                    if(scores[scores.length-2])
                        scores[scores.length-2] = scores[scores.length-2] * 2;
                }
                else {
                    num *= -1;
                }
            }
            scores[scores.length-1] = num;
        }
        else {
            if(dartResult[i+1] && !isNaN(Number(dartResult[i+1]))) {
                scores.push(10);
                i++;
            }
            else scores.push(char);
        }
    }
    
    for(const score of scores) answer += score;
    
    return answer;
}
```

## [1차] 비밀지도

https://school.programmers.co.kr/learn/courses/30/lessons/17681

```js
function solution(n, arr1, arr2) {
    var answer = [];
    
    for(let i=0;i<n;i++) {
        let num = arr1[i] | arr2[i];
        let char = '';
        
        while(num>0) {
            if(num%2 === 0) char += ' ';
            else char += '#';
            num = Math.floor(num/2);
        }
        
        char = char.padEnd(n,' ').split('').reverse().join('');
        answer.push(char);
    }
    
    return answer;
}
```

## 예산

https://school.programmers.co.kr/learn/courses/30/lessons/12982

```js
function solution(d, budget) {
    var answer = 0;
    
    d.sort((a,b) => a-b);
    for(money of d) {
        budget -= money;
        if(budget < 0) break;
        answer++;
    }
    
    return answer;
}
```

## *소수 만들기

https://school.programmers.co.kr/learn/courses/30/lessons/12977

```js
function solution(nums) {
    var answer = 0;
    
    let sum = 0;
    function combination(arr,n,r) {
        for(let i=n;i<arr.length-r+1;i++) {
            if(r === 1) {
                sum += arr[i];
                for(let j=2;j<=sum;j++) {
                    if(j === sum) {
                        answer++;
                        break;
                    }
                    if(sum%j === 0) {
                        break;
                    }
                }
                sum -= arr[i];
            }
            else {
                sum += arr[i];
                combination(arr,i+1,r-1);
                sum -= arr[i];
            }
        }
    }
    combination(nums,0,3)
        
    return answer;
}
```

## 직사각형 별찍기

https://school.programmers.co.kr/learn/courses/30/lessons/12969

```js
process.stdin.setEncoding('utf8');
process.stdin.on('data', data => {
    const n = data.split(" ");
    const a = Number(n[0]), b = Number(n[1]);
    
    let stars = '';
    for(let i=0;i<a;i++) stars += '*';
    for(let i=0;i<b;i++) console.log(stars);

});
```

## x만큼 간격이 있는 n개의 숫자

https://school.programmers.co.kr/learn/courses/30/lessons/12954

```js
function solution(x, n) {
    var answer = [];
    
    for(let i=1;i<=n;i++) answer.push(x*i);

    return answer;
}
```

## 행렬의 덧셈

https://school.programmers.co.kr/learn/courses/30/lessons/12950

```js
function solution(arr1, arr2) {
    var answer = [[]];
    
    answer = arr1.map((arr,i) => arr.map((num,j) => num+arr2[i][j]))
    
    return answer;
}

```

## 핸드폰 번호 가리기

https://school.programmers.co.kr/learn/courses/30/lessons/12948

```js
function solution(phone_number) {
    var answer = '';
    
    answer = phone_number.slice(-4).padStart(phone_number.length,'*');
    
    return answer;
}
```

